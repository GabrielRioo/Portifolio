import { createStitches } from '@stitches/react'

export const {
    config,
    styled,
    css,
    globalCss,
    keyframes,
    getCssText,
    theme,
    createTheme,
} = createStitches({
    theme: {
        colors: {
            ColorBorder: '#CB8834',
            yellow200: '#FFF3C9',
            yellow500: '#FDDD64',
            yellow800: '#CB8834',
        }
    }
})